#include<bits/stdc++.h>
using namespace std;
#define fast ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);

void solve(){
    
    return;
}

int main(){
    fast;
    int tc=1;
    cin >> tc;
    while(tc--) {
        solve();
    }
    return 0;
}